// 1. Ans to the qs no 1
const arr = ['one', 'two', 'three', 'four', 'five'];
for(let i = 0; i < arr.length; i++)
{
    console.log(arr[i]);
}

// 2. ans to the question no 2
for (let i = 5; i <= 50; i++) {
    if (i % 5 === 0) {
      console.log(i);
    }
  }
  

